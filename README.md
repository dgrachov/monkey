# monkey

An interpreted programming language written in go, following the book *How to write an interpreter in go*, written by *Thorsten Ball*.

## Improvement to-do list

## Technical

- [ ] Generalize `readX` functions in `Lexer` struct by passing the `isX` function as a param

## Functional

- [ ] Integers in identifiers (variable/function names)
- [ ] UTF-8/Unicode support
-[ ] Hex/octal number support
